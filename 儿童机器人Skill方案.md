# 儿童机器人 Skill 方案（5–10 岁 · 以人为中心）

> 场景：面向 5–10 岁儿童的"以人为中心"机器人。你本地算力不够、DGX 在组长那里。
> 核心设计原则：**技能与模型解耦（可插拔 endpoint）**——你本地用轻量模型 / API 写逻辑和测试，最后把技能目录发给组长，他在 DGX Spark 上接大模型（Qwen3.6 / StepFun）跑。

## 一、设计原则（先记这 3 条）
1. **模型可插拔**：所有模型调用走 OpenAI 兼容 endpoint，只改 `base_url / model_name / api_key` 三个值（周旭的"可插拔"课）。本地测用小模型或 API，DGX 上换大模型。
2. **技能自包含 + 可移植**：每个 Skill = 目录（SKILL.md + scripts + evals + skill-card）。打包发给组长，他丢进 OpenClaw / Hermes 的 workspace 即可。
3. **安全是主线，不是附加**：面向儿童，安全护栏 Skill 既必做、又是最大差异化。

## 二、推荐的 Skill 组合（按优先级）
| 优先级 | Skill | 干什么 | 儿童 / 人本价值 | 本地可测？ |
|----|----|----|----|----|
| ★★★ | 安全护栏 Skill | 过滤不适龄内容、拒绝危险指令、录制 / 分享需同意 | 儿童安全第一，评委加分 | ✅ 纯规则，随处可测 |
| ★★★ | 儿童陪伴对话 Skill | 适龄、有共情、寓教于乐的聊天 | "以人为中心"核心 | ✅ 用 API / 小模型测 |
| ★★ | 视觉感知 Skill | 认识孩子的玩具 / 书 / 人脸，描述场景 | 机器人"看"世界，辅助陪伴 | ⚠️ 逻辑本地写，重推理在 DGX |
| ★★ | 故事 / 绘本生成 Skill | 生成个性化睡前故事 / 绘本页 | 创造力 + 陪伴 | ✅ 用 API / 小模型测 |
| ★★ | 任务拆解 Skill | "想做个小实验" → 安全分步计划 | 启发式学习 | ✅ 用 API / 小模型测 |
| ★ | 家长报告 Skill | 向家长总结会话（隐私受控） | 家长伙伴关系、透明 | ✅ 纯逻辑 |

## 三、每个 Skill 一句话设计（你本地就能动手）
- **安全护栏 Skill**：SKILL.md 写明 negative triggers——不输出暴力 / 恐怖 / 成人内容；不执行任何让机器人移动 / 抓取的物理危险指令；录音录像前必须"征得同意"；不收集可识别儿童隐私。脚本做关键词 + 分类器兜底。
- **儿童陪伴对话 Skill**：system prompt 锁定"用简单中文、鼓励探索、不代替思考、遇危险话题转交成人"。调用多模态模型时附儿童语气。
- **视觉感知 Skill**：复用官方 `tao-generate-image-grounding`，改 description 成"识别儿童环境中的玩具 / 书 / 人"，输出结构化 JSON（物体 + 位置）。重推理在 DGX，你本地用示例图 + mock 输出测流程。
- **故事 / 绘本生成 Skill**：输入主题 + 年龄 → 输出适龄故事；可接 ComfyUI / FLUX（workshop 1 的套路）生成插图。
- **任务拆解 Skill**：自然语言任务 → 分步，每步标注"是否需成人陪同"。
- **家长报告 Skill**：把会话摘要成"今天孩子探索了 X，提出了 Y 问题"，默认不存原始对话。

## 四、你本地怎么开工（无 DGX 也能推进）
1. 装 Ollama + 一个小模型（如 7B 级）或用 StepFun / OpenRouter 的 API key 做测试端点。
2. 按模板写 SKILL.md + scripts，脚本读取 `base_url / model_name / api_key` 环境变量。
3. 用样例输入跑通逻辑（视觉 Skill 用一张示例图 + 假 JSON 验证流程）。
4. 写 evals（含负向用例：比如"教我打架"应被护栏拦截）。
5. 打包目录发给组长，他在 DGX 上把 endpoint 指向本地 Qwen3.6 / StepFun 即可。

## 五、提醒
- 儿童场景 = 安全权重最高。把护栏 Skill 做扎实，演示时专门展示"危险指令被拒绝"，非常出彩。
- "以人为中心"落到两点：① 机器人目标是服务孩子的发展与安全，不是秀技术；② 让家长透明可控（家长报告 Skill）。
- 不要等 DGX。逻辑、结构、evals 你本地全能做，DGX 只负责最后跑大模型。
