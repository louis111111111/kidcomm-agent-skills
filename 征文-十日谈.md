# 十日谈：从手写 glue 到 Agent Skills（kidcomm 参赛札记）

> 第三届 NVIDIA DGX Spark 黑客松 · Agent Skills 驱动的 Agent 应用。本文记录我们做 kidcomm（亲子引导表达）过程中的认知转变与踩坑，呼应征文主题：Skills 认知转变、仿真→真机坑、安全权衡、模型切换。

## 第一天：我以为要写一堆 glue code
最初想到"让家长听懂孩子"，脑子里全是：调 API、拼提示词、处理语音、做个前端……一堆手写的胶水代码。第一天我就在想怎么把这些串起来。直到认真读 NVIDIA 的 Agent Skills 规范——一个 `SKILL.md` + 脚本 + 评测 就能变成一个**可被 Agent 自动发现、自动调用的能力包**。认知第一次转变：**不是写应用，是写"能力"**。把"引导表达"封装成一个 Skill，自然语言问题进来，结构化的引导计划出去，glue 被 Skill 吃掉了。

## 第三到五天：仿真（逻辑）与真机（大模型）的坑
我们在本地没有 DGX，只能在 Mac 上用轻量逻辑先验证流程：护栏规则、JSON 解析、评测骨架。等真正接上大模型（组长在 DGX Spark 上跑本地多模态），第一个坑来了：**同样的提示词，小模型和大模型产出天差地别**。本地测过的"非诱导"在弱模型上偶尔会冒出"你应该"这种暗示。这让我明白——**仿真（本地逻辑）只验证了流程，真机（大模型）才验证效果**。我们于是把"非诱导"做成评测里的硬断言，而不是靠提示词祈祷。

## 第六到七天：安全是加分项，不是负担
做护栏时我一度觉得"过滤隐私/诱导"是额外工作。但往届规律里"拒绝危险指令本身就是技术深度+加分项"点醒了我。我们把护栏做成**确定性规则**（手机号/身份证/住址正则 + 诱导词表），离线就能跑、不需要模型。演示里让家长误输"问问孩子爸妈几点回家"，护栏直接 block——这段安全桥段反而成了最出彩的部分。**安全不是枷锁，是叙事。** 同时我们也权衡：拦太死会伤表达自由，所以 only 拦明确危险/隐私，普通情绪表达一律放行。

## 第八到九天：模型切换的甜头
一开始绑死某个云端模型，后来发现 DGX 上跑的是本地大模型、开发期用的是另一个端点。幸好我们一开始就用了**可插拔端点**（只改 `KIDCOMM_BASE_URL/MODEL`），逻辑一行没动。这也让我们能提"StepFun 3.7 Flash 可作可选 VLM backbone"而不必重构。切换模型不可怕，**可怕的是把模型写死在代码里**。

## 第十天：验证比演示更重要
最后回头看，最值钱的不是 demo 能跑，而是我们设计了一套**可验证**的路径：护栏有 5/5 离线评测；引导有结构评测；还做了 Wizard-of-Oz pilot 方案（人当机器人去和真实孩子走一遍）和自对弈仿真（模型扮孩子、裁判打分）。多数作品只交 demo，我们交"demo + 怎么证明它有用"。这大概是这个项目最对评审胃口的地方。

## 一句话
Skills 把"能力"从代码里解放出来；仿真验证逻辑、真机验证效果；安全是叙事不是负担；模型要可插拔；**验证，比演示更硬**。
